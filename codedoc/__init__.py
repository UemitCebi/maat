"""CodeDoc — documentation-as-code framework for humans and AI agents.

A zero-dependency (stdlib-only) toolkit that keeps a repository's ``docs/``
tree, its agent-instruction files, and its source code in lockstep.

Public entry point is :func:`codedoc.cli.main`.
"""

__version__ = "0.1.0"
